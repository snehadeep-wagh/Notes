The error occurs because TextLoader cannot read .pdf files.TextLoader is designed strictly for plain-text documents (like .txt or .md). Because a PDF contains binary data, layout styling, and structural encoding, TextLoader throws a RuntimeError when trying to read it.To parse a PDF, you must use a dedicated PDF loader like PyPDFLoader instead.The Solution: Switch to PyPDFLoaderInstall the required PDF parser in your envir

Quick Path Cleanup TipIn your error log, the file path shows mixed slashes (C://Users//Sneh//Downloads//PlushCV.pdf). While Python usually resolves this, it is safest to use a single forward slash (/) or a raw string prefix (r"C:\Users\...") in Windows environments to prevent file system confusion.Once you have successfully loaded your CV, are you planning to extract specific text fields (like skills or experience), or are you building a RAG pipeline to query it?